{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "scripts": {
    "build": "prisma generate && npm run build:function"
  },
  "routes": [{ "src": "/(.*)", "dest": "index.js" }]
}
